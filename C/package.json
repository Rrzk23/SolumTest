{
  "name": "frontend-login",
  "private": true,
  "version": "1.0.0",
  "description": "Simple login page (no backend). Optional dev server for local preview.",
  "scripts": {
    "dev": "http-server -c-1 -p 5173",
    "start": "npm run dev"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}